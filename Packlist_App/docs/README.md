# Docs

Use this folder for project documentation and non-code reference material.

- `exports/` - exported chats, transcripts, or reports
- `data/` - reference datasets, notes, or import/export artifacts

Keep app runtime files in `uploads/` and `outputs/` (already ignored by git).
